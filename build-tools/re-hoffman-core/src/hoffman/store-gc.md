R""(

# Examples

* Delete unreachable paths in the Hoffman store:

  ```console
  # hoffman store gc
  ```

* Delete up to 1 gigabyte of garbage:

  ```console
  # hoffman store gc --max 1G
  ```

# Description

This command deletes unreachable paths in the Hoffman store.

)""
